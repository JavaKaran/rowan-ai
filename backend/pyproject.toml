[project]
name = "backend"
version = "0.1.0"
description = "AI SQL Query"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi[standard]",
    "python-dotenv",
    "pydantic",
    "langchain>=1.3.14",
    "langchain-groq>=1.1.3",
]
